import { combineReducers } from 'redux'; // import combineReducers
import posts from './posts';

// this is combine needed reducers and export.
// index file is endded until combining.

export default combineReducers({ posts });  // export with combine with posts reducers and ... 
